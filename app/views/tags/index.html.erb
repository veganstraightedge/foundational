<div class='container'>
  <h1 class='mb-3 font-weight-light'>Tags</h1>

  <ol class='list-inline'>
    <% @tags.each do |tag| %>
      <%= link_to tag.name, tag, class: 'btn btn-outline-primary mb-3 mr-3' %>
    <% end %>
  </ol>
</div>
