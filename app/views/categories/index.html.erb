<div class='container'>
  <h1 class='mb-3 font-weight-light'>Categories</h1>

  <ol class='list-inline'>
    <% @categories.each do |category| %>
      <%= link_to category.name, category, class: 'btn btn-outline-primary mb-3 mr-3' %>
    <% end %>
  </ol>
</div>
